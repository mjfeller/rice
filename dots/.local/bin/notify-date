#!/bin/sh
# Display the date using notify-send
# A nice alternative to displaying the time on a bar.

# Arbitrary but unique message id
msgId="661049"

dunstify -r $msgId -t 5000 "$(date +"%A, %-d %b %-I:%M")"
